{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Mix Default Compile",
      "type": "shell",
      "command": "mix clean && mix deps.get && mix deps.compile && mix phx.server",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "Mix Run Server",
      "type": "shell",
      "command": "mix phx.server",
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}

